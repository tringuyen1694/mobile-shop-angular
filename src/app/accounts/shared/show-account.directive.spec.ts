import { ShowAccountDirective } from './show-account.directive';

describe('ShowAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAccountDirective();
    expect(directive).toBeTruthy();
  });
});
